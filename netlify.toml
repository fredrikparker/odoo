# Netlify build settings for the frontend. Adjust "base" if your frontend lives in a subfolder.
[build]
  base = "frontend" # set to "" if the frontend is at repo root
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
