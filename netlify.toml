# Netlify build settings. No frontend present yet, so use a no-op build to avoid failures.
[build]
  base = ""
  command = "echo \"No frontend to build\""
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
